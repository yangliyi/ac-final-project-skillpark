<%= form_for(@admin_publiccomment) do |f| %>
  <% if @admin_publiccomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_publiccomment.errors.count, "error") %> prohibited this admin_publiccomment from being saved:</h2>

      <ul>
      <% @admin_publiccomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
